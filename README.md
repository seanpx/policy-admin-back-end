# Policy Admin Back End

Spring Boot (Maven) service.

## Build

```bash
./mvnw clean package
```

On Windows:

```bat
mvnw.cmd clean package
```

## Run

```bash
./mvnw spring-boot:run
```

The app reads configuration from `src/main/resources/application.yml`.

